services:
  app:
    build: .
    ports:
      - "${BACKEND_PORT:-8000}:${BACKEND_PORT:-8000}"
    env_file:
      - .env
    environment:
      - DB_PATH=/data/dashboard.db
    volumes:
      - soporte_data:/data

volumes:
  soporte_data:
